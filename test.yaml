- name: raw
      value: |
        {{- /* 1. Load the file and parse it into an Object (Data) */ -}}
        {{- $jobData := .Files.Get "files/verification-external-dns.yaml" | fromYaml -}}
        
        {{- /* 2. Mimic the 'Factory Pattern' from apps.yaml: Add Separator + Safe Indentation */ -}}
        ---
        {{- $jobData | toYaml | nindent 10 }}