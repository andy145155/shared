- name: raw
      value: |
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: debug-job-test
          namespace: af-toolkit
          annotations:
            argocd.argoproj.io/hook: PostSync
        spec:
          template:
            spec:
              containers:
              - name: test
                image: alpine
                command: ["echo", "hello"]
              restartPolicy: Never