- name: raw
      value: |
        {{- /* Load the file as a LIST */ -}}
        {{- $jobList := .Files.Get "files/verification-external-dns.yaml" | fromYaml -}}
        
        {{- /* COPY OF APPS.YAML LOGIC: Loop through the list */ -}}
        {{- range $jobList }}
        {{- "---" | nindent 10 }}       {{- /* Safely adds the separator */ -}}
        {{- . | toYaml | nindent 10 }}  {{- /* Formats the job */ -}}
        {{- end }}