- name: raw
      value: |
        {{- /* We use .Files.Get to read the clean YAML file, 
           and nindent to safely inject it into this block.
           This mimics the safety of the apps.yaml 'toYaml | nindent' pattern.
        */ -}}
        {{- .Files.Get "files/verification-external-dns.yaml" | nindent 10 }}